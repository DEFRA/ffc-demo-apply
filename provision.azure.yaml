resources:
  identity: ffc-demo-apply
  # topics:
  #   - name: ffc-demo-apply
  #     queues:
  #       - name: ffc-demo-apply
  #         role: sender/reciever
  #         subscription:
  #           name: subscription_name
  queues:
    - name: ffc-demo-apply
      role: sender
      # subscription:
      #       name: subscription_name
  postgreSqls:
    - name: ffc-demo-apply
  # storages:
  #   - name: ffcdemoapplystorage
  # redis: 
  #   name: default
  # accessTo:
  #     postgreSqls:
  #       - name: some-database
      
     